name: publish nuget

on: 
  push:
    branches: [main]

jobs:
  build:
    strategy: 
        matrix: 
         project: [wwtp_paas_main_bus_service]
    runs-on: ubuntu-latest
    env:
        NUGET_API_KEY: ${{secrets.NUGET_API_KEY}}  # NUGET_API_KEY要作为一个secret创建
    steps:
      - name: pre -checkout 
        run: |
            pwd
            ls -lh
      - name: checkout code
        uses: actions/checkout@v3
      - name: after -checkout 
        run: |
            pwd
            ls -lh
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
            dotnet-version: '6.0.x'  # .NET版本
      - name: Restore dependencies
        working-directory: src/${{ matrix.project }} 
        run: dotnet restore 
      - name: Build
        working-directory: src/${{ matrix.project }} 
        run: |
            pwd
            ls -lh
            dotnet build  --configuration Release --no-restore
      - name: Pack 
        working-directory: src/${{ matrix.project }} 
        run: dotnet pack  -c Release -o nupkgs  
      - name: Push packages 
        working-directory: src/${{ matrix.project }} 
        run: dotnet nuget push "nupkgs/*.nupkg" -s https://api.nuget.org/v3/index.json -k ${{secrets.NUGET_API_KEY}}


